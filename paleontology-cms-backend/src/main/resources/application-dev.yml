spring:
  datasource:
    url: jdbc:mysql://localhost:3306/paleo_cms?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456t
    driver-class-name: com.mysql.cj.jdbc.Driver
  # Redis 可选：无 Redis 时取消下面 exclude 注释即可启用 JWT 黑名单
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 3000ms

logging:
  level:
    com.chuanghai.paleo.cms: debug
